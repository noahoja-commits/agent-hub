#!/usr/bin/env python3
"""Agent Hub CLI v2 — deep pass with memory, live updates, file output."""
# This file is auto-generated from agent — see that file for the full source.
# The CLI now supports: --watch (live task feed), --file (download output)
import sys, os
sys.path.insert(0, os.path.dirname(__file__))
from agent import main
if __name__ == "__main__":
    main()
